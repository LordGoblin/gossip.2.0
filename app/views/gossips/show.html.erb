<div class="d-flex flex-column">
	<div>
		<h1>voila le gossip numero : <%= @goss.id %></h1>
	</div>
	<div>
		<h3>titre : <%= @goss.title %></h2>
		<p><%= @goss.content %></p>
		<p>crée le : <%= @goss.created_at %></p>
		<br/>
		<br/>
		<a class="" href="/users/<%= @goss.user.id %>">potin de : <%= @goss.user.first_name %></a>
		<a href="" class="btn btn-danger" data-confirm="sur?" data-method="delete">delete</a>
	</div>
	<div>
		<br /> <br />
		<a class="btn btn-primary" href="<%= new_gossip_comment_path(@goss.id) %>">Comment</a>
		<p>nombre de comment : <%= @nombre %></p>
		<br><br>
	</div>
	<div class="d-flex flex-column">
		<% @com.each do |com| %>
    		<div class="card mb-4 bg-success">
        		<div class="card-header border border-dark ">
        			<a class="text-warning" href=""><%= com.content %><br/></a>
        			<a class="text-warning" href=""><%= com.user.first_name %><br/></a>
        			<a class="pr-5 text-warning" href="<%= edit_gossip_comment_path(@goss.id, com.id) %>">edit commentaire</a>
        		</div>
    		</div>
   		<% end %>
	</div>
</div>